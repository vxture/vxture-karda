# 40-implementation - Package guides, coding rules, dev setup

Layer/package guides, coding conventions, and local dev setup for this repo.

Empty so far. The inherited workspace packages (`@karda/app`, `@karda/shared`)
carry the platform integration shell and need no karda-specific guide yet; this
decade fills in as karda's own packages and conventions appear. Each package
carries a thin `AGENTS.md` at its root pointing back to `docs/`, with the
substantive guidance here under `NN-slug.md`.
