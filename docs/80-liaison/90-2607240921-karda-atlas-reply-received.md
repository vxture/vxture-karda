# karda 收悉：Atlas 现状与能力回复（对 `70-2607232158` 的回函处理）

> **记录方**：vxture-karda
> **回函来源**：vxture-platform（Atlas 尚未独立为产品/仓库，无独立 atlas 线，故由 platform 出具）
> **时间**：2026-07-24 09:21（stamp 2607240921）
> **性质**：**收悉 + 计划调整触发**，非仅存档——本回复改变了批次 5/6 的前提
> **前序**：`70-2607232158-karda-atlas-contract-request.md`（发时误设收件方为"atlas 线"）

---

## 1. 回复要点（平台线出具）

| 事项 | 平台线答复 |
|---|---|
| Atlas 独立性 | **尚未独立**。现以 `@vxture/service-model-platform`（`services/model/platform`，vxture-platform 仓内）合并实现存在；`40-model-platform.md` §11 Stage 1 明标"早期合并实现，非终态域名" |
| A4 生成 | **已实现且生产运行**：`ChatRequest` 契约、租户/应用授权、配额校验、计量、fallback 路由齐备 |
| A1 embedding | **未实现**——能力本身不存在，非缺文档 |
| A2 解析类小模型 | **未实现** |
| A3 rerank | **未实现** |
| karda 假定的语义（service 凭证姿态、按资产归属计量、429 限流/配额区分） | **尚无对应设计** |
| A1–A3 交付 | 已登记为 Atlas 目标能力，但需**先设计再实现**，非短期可交付的"补文档"工作 |

## 2. 对 karda 的实际影响（这是本记录存在的理由）

`70` 号函把 Atlas 列为"批次 5/6 的前置、最长外部依赖"。回复证实这个判断，但把依赖的**性质**从"等一份契约文档"改成了"**等一项尚不存在的能力被设计并实现**"——量级完全不同。

具体到四项：

- **A4（生成）就绪** → `karda.ask` 的应答域（批次 6 的一部分）**可以真实推进**，不必 mock。`ChatRequest` 契约在 `40-model-platform.md` 已定形（`modelCode` / `messages` / `tenantId` / `applicationId+applicationType` / `usageType` 等）。
- **A1（embedding）未实现** → 批次 5 加工管线的**向量化主路径无法真实跑通**。这是最硬的阻塞：没有 embedding，文档进不了向量索引，检索域也就没有可召回的东西。
- **A2/A3 未实现** → 深度解析（版面/OCR/表格）与统一精排同样只能建骨架 + mock，等能力到位。

**结论**：karda 不能再把"Atlas 契约到位"当作批次 5/6 的近期解锁点。批次 5/6 的**真实跑通**要跟 Atlas 的能力建设排期,而非 karda 自身进度。

## 3. karda 侧的应对（见配套决策与计划调整）

1. **KD-101/102（Atlas 依赖）**状态从"待契约"改为"**待能力建设**"，并拆分：A4 已解除、A1/A2/A3 阻塞升级；
2. **批次顺序调整**:把 batch 6 中**不依赖 A1 的部分**(应答域 `karda.ask` 走 A4、检索求值链骨架、可见集缓存、门控/CTA)提前;把**依赖 embedding 的部分**(向量化、真实召回)显式标注为 Atlas-blocked;
3. 向量存储选型(批次 6 的自建向量库)**与 embedding 能力解耦推进**——存储层可先建,等 A1 到位再灌向量;
4. `70` 号函的收件方误设已知悉:此后 Atlas 相关联络经 **platform 线**,不另设 atlas 收件方。

## 4. 需回平台线的一件事

karda 会提交一份 **A1/A2/A3 的能力需求清单**(karda 侧期望的接口形态、embedding 的版本锁定诉求、rerank 的候选池规模与延迟预算、解析类的批量/部署亲和诉求),作为 Atlas 设计这三项时的输入——把 `70` 号函 §2/§3 的诉求转成 Atlas 可直接消费的需求条目。这不阻塞 karda 当前工作,但越早给 Atlas 越有利于其排期。
