// Tailwind 4 via PostCSS - required by @vxture/design-system (KD-020 adoption).
// Same shape as the platform portals' config.
module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
